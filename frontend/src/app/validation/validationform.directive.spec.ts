import { ValidationformDirective } from './validationform.directive';

describe('ValidationformDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidationformDirective();
    expect(directive).toBeTruthy();
  });
});
