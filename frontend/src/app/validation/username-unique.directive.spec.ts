import { UsernameUniqueDirective } from './username-unique.directive';

describe('UsernameUniqueDirective', () => {
  it('should create an instance', () => {
    const directive = new UsernameUniqueDirective();
    expect(directive).toBeTruthy();
  });
});
